# Convert csv files with twitter handles into Twitter lists

## Environment (settings.json)

```
{
	"private" : {
		"twitter" : {
			"consumerKey" : "twitter-consumer-key",
			"secret"      : "twitter-consumer-secret"
		}
	},

	"public" : {
		"maxListSize" : 50
	}
}
```